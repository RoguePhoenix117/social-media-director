# pnpm 11 reads auth/registry from .npmrc only; supply-chain policy lives in
# pnpm-workspace.yaml (minimumReleaseAge + minimumReleaseAgeStrict).
#
# npm CLI 11.10+ equivalent when using npm install (value is days):
min-release-age=1
